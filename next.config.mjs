/** @type {import('next').NextConfig} */
const nextConfig = {
	output: "export",
	// basePath: "/nextjs-github-pages",
	basePath: "/home",
	images: {
		unoptimized: true
	}
}

export default nextConfig
